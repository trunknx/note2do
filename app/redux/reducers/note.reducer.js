import {} from '../actions/index.actions';

const defaultState = {
	text: '',
	title: ''
};

const note = (state = defaultState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default note;
